controller:
  image:
    registry: docker.io
    repository: jenkins/jenkins
    tag: lts
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1000m"
      memory: "2Gi"
  serviceType: LoadBalancer
  ingress:
    enabled: false
persistence:
  enabled: true
  storageClass: "ebs-sc"
  size: 8Gi
